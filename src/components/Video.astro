---
interface Props {
  src: string;
  loop?: boolean;
}

const { src, loop = true } = Astro.props;
const videoSrc = `/videos/${src}`;
---

<video
  src={videoSrc}
  controls
  autoplay
  loop={loop || undefined}
  muted
  playsinline
  class="block w-full"></video>
